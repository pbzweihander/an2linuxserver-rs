use anyhow::Result;
use std::path::Path;

pub fn generate_certificate_and_rsa_private_key_to_path(
    certificate_path: &Path,
    rsa_private_key_path: &Path,
) -> Result<()> {
    // TODO: Generate certificate and private key
    unimplemented!()
}
